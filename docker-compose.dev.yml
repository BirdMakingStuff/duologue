services:
  duologue:
    image: duologue:dev
    build:
      context: .
      target: release
    volumes:
      - ./config.toml:/usr/src/app/config.toml:ro
      - duologue-data:/usr/src/app/data
    restart: unless-stopped

volumes:
  duologue-data:
