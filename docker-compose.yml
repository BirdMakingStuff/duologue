services:
  duologue:
    image: ghcr.io/birdmakingstuff/duologue:latest
    pull_policy: always
    ports:
      - "3000:3000"
    volumes:
      - ./config.toml:/usr/src/app/config.toml:ro
      - duologue-data:/usr/src/app/data
    restart: unless-stopped

volumes:
  duologue-data: